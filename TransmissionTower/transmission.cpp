#include <string>
#include <vector>
#include <iostream>

using namespace std;

vector<int> solution(vector<int> heights) {
    vector<int> answer;

    return answer;
}

int main()
{
    vector<int> v{6,9,5,7,4};
    cout<<"Transmission of each towers / Hoon"<<endl;
    cout << "The array of Received Tower: [" ;
    v = solution(v);
    for (int i = 0; i < v.size(); ++i)
    {
        cout<<v[i]<< " ";
    }
    cout<<"]"<<endl;

    return 0;
}
